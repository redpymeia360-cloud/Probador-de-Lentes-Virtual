import { useState, useEffect, useRef } from 'react';
import {
  Brain,
  TrendingUp,
  Users,
  Zap,
  CheckCircle,
  ArrowRight,
  MessageCircle,
  Mail,
  Phone,
  Star,
  ChevronDown,
  BarChart3,
  Cpu,
  Globe,
  Shield,
  Rocket,
  Menu,
  X,
  Package,
  Crown,
  Sparkles,
  Palette,
  Video,
  CreditCard,
  FileImage,
} from 'lucide-react';

const WHATSAPP_NUMBER = '56921733645';
const WHATSAPP_LINK = `https://wa.me/${WHATSAPP_NUMBER}`;
const WHATSAPP_DIAGNOSTICO = `https://wa.me/${WHATSAPP_NUMBER}?text=${encodeURIComponent('Hola, me interesa el Diagnostico Gratis para mi PyME. Me gustaria agendar una sesion.')}`;

function useIntersectionObserver(threshold = 0.15) {
  const ref = useRef<HTMLDivElement>(null);
  const [visible, setVisible] = useState(false);

  useEffect(() => {
    const el = ref.current;
    if (!el) return;
    const observer = new IntersectionObserver(
      ([entry]) => {
        if (entry.isIntersecting) {
          setVisible(true);
          observer.disconnect();
        }
      },
      { threshold }
    );
    observer.observe(el);
    return () => observer.disconnect();
  }, [threshold]);

  return { ref, visible };
}

const services = [
  {
    icon: Brain,
    title: 'Automatizacion con IA',
    description:
      'Implementamos flujos de trabajo inteligentes que eliminan tareas repetitivas y liberan a tu equipo para lo que realmente importa.',
    color: 'text-sky-600',
    bg: 'bg-sky-50',
    border: 'border-sky-100',
  },
  {
    icon: BarChart3,
    title: 'Analisis de Datos',
    description:
      'Convertimos tus datos en decisiones. Dashboards en tiempo real, predicciones de demanda y reportes automatizados para tu PyME.',
    color: 'text-emerald-600',
    bg: 'bg-emerald-50',
    border: 'border-emerald-100',
  },
  {
    icon: MessageCircle,
    title: 'Chatbots y Asistentes',
    description:
      'Atencion al cliente 24/7 con chatbots inteligentes entrenados en tu negocio. Responden, califican leads y venden mientras duermes.',
    color: 'text-amber-600',
    bg: 'bg-amber-50',
    border: 'border-amber-100',
  },
  {
    icon: Globe,
    title: 'Presencia Digital',
    description:
      'Sitios web optimizados, estrategia SEO y marketing digital potenciado por IA para que te encuentren tus clientes ideales.',
    color: 'text-rose-600',
    bg: 'bg-rose-50',
    border: 'border-rose-100',
  },
  {
    icon: Cpu,
    title: 'Integracion de Sistemas',
    description:
      'Conectamos tus herramientas existentes: CRM, ERP, facturacion y mas, con flujos automatizados y sin fricciones.',
    color: 'text-violet-600',
    bg: 'bg-violet-50',
    border: 'border-violet-100',
  },
  {
    icon: Shield,
    title: 'Consultoria Estrategica',
    description:
      'Diagnostico digital gratuito y hoja de ruta personalizada para que tu PyME adopte la IA de forma gradual y sin riesgos.',
    color: 'text-teal-600',
    bg: 'bg-teal-50',
    border: 'border-teal-100',
  },
];

const benefits = [
  { value: '3x', label: 'mas productividad promedio' },
  { value: '40%', label: 'reduccion de costos operativos' },
  { value: '24/7', label: 'atencion automatizada' },
  { value: '+200', label: 'PyMEs transformadas' },
];

const testimonials = [
  {
    name: 'Maria Gonzalez',
    role: 'Directora, Boutique MG',
    text: 'Gracias a su chatbot automatizamos el 70% de las consultas de clientes. Triplicamos ventas online en 4 meses sin contratar mas personal.',
    stars: 5,
  },
  {
    name: 'Carlos Herrera',
    role: 'CEO, Distribuidora Herrera',
    text: 'El sistema de analisis de inventario nos ahorra 15 horas semanales. La prediccion de demanda redujo nuestro stock muerto en un 35%.',
    stars: 5,
  },
  {
    name: 'Ana Lucia Torres',
    role: 'Fundadora, Estudio AT',
    text: 'Tenia miedo de la IA pero el equipo nos guio paso a paso. Ahora automatizamos presupuestos, recordatorios y seguimiento de clientes.',
    stars: 5,
  },
];

const steps = [
  {
    num: '01',
    title: 'Diagnostico Gratuito',
    desc: 'Analizamos tu negocio, procesos y objetivos en una sesion de 45 minutos sin costo ni compromiso.',
  },
  {
    num: '02',
    title: 'Hoja de Ruta Personalizada',
    desc: 'Diseñamos un plan a medida con las soluciones de IA que mayor impacto tendran en tu PyME.',
  },
  {
    num: '03',
    title: 'Implementacion Agil',
    desc: 'Ejecutamos en sprints cortos para que veas resultados reales desde las primeras semanas.',
  },
  {
    num: '04',
    title: 'Acompañamiento Continuo',
    desc: 'Monitoreo, optimizacion y soporte permanente para que tu inversion en IA siga creciendo.',
  },
];

const plans = [
  {
    name: 'Inicio Digital',
    subtitle: 'Perfecto para PyMEs que dan el primer paso',
    price: '$89.000',
    period: '/mes',
    icon: Package,
    color: 'sky',
    popular: false,
    features: [
      'Sitio web profesional responsivo',
      'Chatbot basico para WhatsApp',
      'Automatizacion de respuestas frecuentes',
      'Dashboard de metricas basicas',
      'Soporte por email (48h)',
      '1 sesion mensual de seguimiento',
    ],
  },
  {
    name: 'Crecimiento IA',
    subtitle: 'El plan mas elegido por PyMEs en expansion',
    price: '$189.000',
    period: '/mes',
    icon: Sparkles,
    color: 'sky',
    popular: true,
    features: [
      'Todo lo del plan Inicio Digital',
      'Chatbot avanzado con IA conversacional',
      'Automatizacion de procesos (CRM + ERP)',
      'Analisis predictivo de ventas',
      'SEO y marketing digital con IA',
      'Soporte prioritario (12h)',
      '2 sesiones mensuales de estrategia',
      'Reportes automatizados semanales',
    ],
  },
  {
    name: 'Transformacion Total',
    subtitle: 'Para PyMEs que quieren liderar su industria',
    price: '$349.000',
    period: '/mes',
    icon: Crown,
    color: 'sky',
    popular: false,
    features: [
      'Todo lo del plan Crecimiento IA',
      'Asistente IA personalizado para tu negocio',
      'Integracion completa de sistemas',
      'Modelo predictivo a medida',
      'Automatizacion avanzada multi-canal',
      'Soporte dedicado 24/7',
      'Semanas de estrategia ilimitadas',
      'Training para tu equipo',
      'API personalizada',
    ],
  },
];

const extraServices = [
  {
    name: 'Landing Page Express',
    desc: 'Diseño, contenido y publicacion en 5 dias habiles',
    price: '$149.000',
    tag: 'Pago unico',
    icon: Globe,
  },
  {
    name: 'Chatbot WhatsApp Pro',
    desc: 'Bot inteligente con flujo de ventas y calificacion de leads',
    price: '$119.000',
    tag: 'Pago unico',
    icon: MessageCircle,
  },
  {
    name: 'Auditoria Digital Completa',
    desc: 'Analisis de 360 grados de tu presencia y procesos digitales',
    price: '$79.000',
    tag: 'Pago unico',
    icon: BarChart3,
  },
  {
    name: 'Pack Redes Sociales IA',
    desc: '30 posts mensuales generados con IA + calendario editorial',
    price: '$69.000',
    tag: '/mes',
    icon: Brain,
  },
];

const quickServices = [
  {
    name: 'Creacion de Logo',
    desc: 'Logo profesional y memorable para tu marca. Incluye 3 propuestas, formatos PNG/SVG y manual de uso basico.',
    price: '$8.000',
    icon: Palette,
    color: 'from-rose-500 to-orange-400',
    bg: 'bg-rose-50',
    border: 'border-rose-100',
  },
  {
    name: 'Video Comercial',
    desc: 'Video publicitario de 30–60 seg con guion, edicion profesional, musica y subtitulos. Listo para redes.',
    price: '$25.000',
    icon: Video,
    color: 'from-sky-500 to-cyan-400',
    bg: 'bg-sky-50',
    border: 'border-sky-100',
  },
  {
    name: 'Chatbot Express',
    desc: 'Chatbot funcional para WhatsApp o web en 48 horas. Flujo basico de bienvenida, faq y captura de datos.',
    price: '$18.000',
    icon: MessageCircle,
    color: 'from-emerald-500 to-teal-400',
    bg: 'bg-emerald-50',
    border: 'border-emerald-100',
  },
  {
    name: 'Flyer Digital',
    desc: 'Diseño de flyer o propaganda digital para campañas. Incluye 2 revisiones y formatos para redes e impresion.',
    price: '$9.000',
    icon: FileImage,
    color: 'from-amber-500 to-yellow-400',
    bg: 'bg-amber-50',
    border: 'border-amber-100',
  },
  {
    name: 'Tarjetas de Presentacion',
    desc: 'Diseño de tarjetas profesionales con tu identidad visual. Incluye frente y dorso, archivo para imprenta.',
    price: '$12.000',
    icon: CreditCard,
    color: 'from-violet-500 to-fuchsia-400',
    bg: 'bg-violet-50',
    border: 'border-violet-100',
  },
];

export default function App() {
  const [menuOpen, setMenuOpen] = useState(false);
  const [scrolled, setScrolled] = useState(false);
  const [formData, setFormData] = useState({ name: '', email: '', phone: '', message: '' });
  const [submitted, setSubmitted] = useState(false);

  const heroSection = useIntersectionObserver(0);
  const servicesSection = useIntersectionObserver();
  const benefitsSection = useIntersectionObserver();
  const quickServicesSection = useIntersectionObserver();
  const stepsSection = useIntersectionObserver();
  const testimonialsSection = useIntersectionObserver();
  const contactSection = useIntersectionObserver();

  useEffect(() => {
    const onScroll = () => setScrolled(window.scrollY > 30);
    window.addEventListener('scroll', onScroll);
    return () => window.removeEventListener('scroll', onScroll);
  }, []);

  const scrollTo = (id: string) => {
    document.getElementById(id)?.scrollIntoView({ behavior: 'smooth' });
    setMenuOpen(false);
  };

  const handleSubmit = (e: React.FormEvent) => {
    e.preventDefault();
    setSubmitted(true);
  };

  const navLinks = [
    { label: 'Servicios', id: 'servicios' },
    { label: 'Precios', id: 'precios' },
    { label: 'Como Funciona', id: 'proceso' },
    { label: 'Testimonios', id: 'testimonios' },
    { label: 'Contacto', id: 'contacto' },
  ];

  return (
    <div className="font-sans antialiased bg-white text-gray-900 overflow-x-hidden">
      {/* NAV */}
      <header
        className={`fixed top-0 left-0 right-0 z-50 transition-all duration-300 ${
          scrolled ? 'bg-white shadow-md py-3' : 'bg-transparent py-5'
        }`}
      >
        <div className="max-w-7xl mx-auto px-6 flex items-center justify-between">
          <button
            onClick={() => scrollTo('hero')}
            className="flex items-center gap-2.5 group"
          >
            <div className="w-9 h-9 rounded-xl bg-gradient-to-br from-sky-500 to-cyan-400 flex items-center justify-center shadow-sm">
              <Rocket className="w-5 h-5 text-white" />
            </div>
            <span
              className={`text-lg font-bold tracking-tight transition-colors ${
                scrolled ? 'text-gray-900' : 'text-white'
              }`}
            >
              Impulsamos<span className={scrolled ? 'text-sky-500' : 'text-sky-300'}>TuPyme</span>
              <span className={scrolled ? 'text-cyan-500' : 'text-cyan-300'}>IA</span>
            </span>
          </button>

          {/* Desktop nav */}
          <nav className="hidden md:flex items-center gap-8">
            {navLinks.map((l) => (
              <button
                key={l.id}
                onClick={() => scrollTo(l.id)}
                className={`text-sm font-medium transition-colors hover:text-sky-400 ${
                  scrolled ? 'text-gray-600' : 'text-white/80'
                }`}
              >
                {l.label}
              </button>
            ))}
            <a
              href={WHATSAPP_DIAGNOSTICO}
              target="_blank"
              rel="noopener noreferrer"
              className="bg-sky-500 hover:bg-sky-600 text-white text-sm font-semibold px-5 py-2.5 rounded-xl transition-all hover:shadow-lg hover:shadow-sky-200 active:scale-95"
            >
              Diagnostico Gratis
            </a>
          </nav>

          {/* Mobile hamburger */}
          <button
            className={`md:hidden ${scrolled ? 'text-gray-700' : 'text-white'}`}
            onClick={() => setMenuOpen(!menuOpen)}
          >
            {menuOpen ? <X className="w-6 h-6" /> : <Menu className="w-6 h-6" />}
          </button>
        </div>

        {/* Mobile menu */}
        {menuOpen && (
          <div className="md:hidden bg-white border-t border-gray-100 shadow-lg">
            <div className="max-w-7xl mx-auto px-6 py-4 flex flex-col gap-3">
              {navLinks.map((l) => (
                <button
                  key={l.id}
                  onClick={() => scrollTo(l.id)}
                  className="text-left text-gray-700 font-medium py-2 border-b border-gray-50"
                >
                  {l.label}
                </button>
              ))}
              <a
                href={WHATSAPP_DIAGNOSTICO}
                target="_blank"
                rel="noopener noreferrer"
                className="mt-2 bg-sky-500 text-white font-semibold py-3 rounded-xl text-center"
              >
                Diagnostico Gratis
              </a>
            </div>
          </div>
        )}
      </header>

      {/* HERO */}
      <section
        id="hero"
        className="relative min-h-screen flex items-center justify-center overflow-hidden bg-gradient-to-br from-slate-900 via-sky-950 to-cyan-950"
      >
        {/* Ambient blobs */}
        <div className="absolute top-1/4 left-1/4 w-96 h-96 bg-sky-500/20 rounded-full blur-3xl pointer-events-none" />
        <div className="absolute bottom-1/4 right-1/4 w-80 h-80 bg-cyan-400/15 rounded-full blur-3xl pointer-events-none" />
        <div className="absolute top-1/2 left-1/2 -translate-x-1/2 -translate-y-1/2 w-[600px] h-[600px] bg-sky-600/10 rounded-full blur-3xl pointer-events-none" />

        {/* Grid pattern */}
        <div
          className="absolute inset-0 opacity-10"
          style={{
            backgroundImage:
              'linear-gradient(rgba(255,255,255,.08) 1px, transparent 1px), linear-gradient(90deg, rgba(255,255,255,.08) 1px, transparent 1px)',
            backgroundSize: '60px 60px',
          }}
        />

        <div
          ref={heroSection.ref}
          className={`relative z-10 max-w-5xl mx-auto px-6 text-center transition-all duration-1000 ${
            heroSection.visible ? 'opacity-100 translate-y-0' : 'opacity-0 translate-y-8'
          }`}
        >
          <div className="inline-flex items-center gap-2 bg-sky-500/20 border border-sky-400/30 text-sky-300 text-sm font-medium px-4 py-2 rounded-full mb-8">
            <Zap className="w-4 h-4" />
            Transformacion Digital para PyMEs
          </div>

          <h1 className="text-5xl md:text-7xl font-extrabold text-white leading-tight mb-6 tracking-tight">
            Impulsamos tu PyME
            <br />
            <span className="bg-gradient-to-r from-sky-400 to-cyan-300 bg-clip-text text-transparent">
              con Inteligencia Artificial
            </span>
          </h1>

          <p className="text-xl text-sky-100/70 max-w-2xl mx-auto mb-10 leading-relaxed">
            Automatizamos procesos, analizamos datos y digitalizamos tu negocio para que crezcas
            mas rapido, gastes menos y compitas con las grandes empresas.
          </p>

          <div className="flex flex-col sm:flex-row gap-4 justify-center">
            <a
              href={WHATSAPP_DIAGNOSTICO}
              target="_blank"
              rel="noopener noreferrer"
              className="group inline-flex items-center justify-center gap-2 bg-sky-500 hover:bg-sky-400 text-white font-bold px-8 py-4 rounded-2xl transition-all hover:shadow-2xl hover:shadow-sky-500/30 hover:-translate-y-0.5 active:scale-95"
            >
              Obtener Diagnostico Gratis
              <ArrowRight className="w-5 h-5 group-hover:translate-x-1 transition-transform" />
            </a>
            <button
              onClick={() => scrollTo('servicios')}
              className="inline-flex items-center justify-center gap-2 border border-white/20 text-white/80 hover:text-white hover:border-white/40 font-semibold px-8 py-4 rounded-2xl transition-all hover:bg-white/5"
            >
              Ver Servicios
            </button>
          </div>

          <div className="mt-20 grid grid-cols-2 md:grid-cols-4 gap-6">
            {benefits.map((b, i) => (
              <div
                key={i}
                className="bg-white/5 border border-white/10 rounded-2xl p-5 backdrop-blur-sm hover:bg-white/8 transition-colors"
              >
                <div className="text-3xl font-extrabold text-sky-300 mb-1">{b.value}</div>
                <div className="text-sm text-sky-100/60 leading-snug">{b.label}</div>
              </div>
            ))}
          </div>
        </div>

        <button
          onClick={() => scrollTo('servicios')}
          className="absolute bottom-8 left-1/2 -translate-x-1/2 text-white/40 hover:text-white/70 transition-colors animate-bounce"
        >
          <ChevronDown className="w-6 h-6" />
        </button>
      </section>

      {/* SERVICES */}
      <section id="servicios" className="py-24 bg-gray-50">
        <div className="max-w-7xl mx-auto px-6">
          <div
            ref={servicesSection.ref}
            className={`text-center mb-16 transition-all duration-700 ${
              servicesSection.visible ? 'opacity-100 translate-y-0' : 'opacity-0 translate-y-6'
            }`}
          >
            <span className="inline-block text-sky-600 font-semibold text-sm uppercase tracking-widest mb-3">
              Nuestros Servicios
            </span>
            <h2 className="text-4xl md:text-5xl font-extrabold text-gray-900 mb-4 leading-tight">
              Todo lo que tu PyME necesita
              <br />
              <span className="text-sky-500">para crecer con IA</span>
            </h2>
            <p className="text-lg text-gray-500 max-w-2xl mx-auto">
              Soluciones practicas, medibles y adaptadas a tu presupuesto y sector.
            </p>
          </div>

          <div className="grid md:grid-cols-2 lg:grid-cols-3 gap-6">
            {services.map((s, i) => (
              <div
                key={i}
                style={{ transitionDelay: `${i * 80}ms` }}
                className={`group bg-white rounded-2xl border ${s.border} p-7 hover:shadow-xl hover:-translate-y-1 transition-all duration-300 ${
                  servicesSection.visible ? 'opacity-100 translate-y-0' : 'opacity-0 translate-y-8'
                }`}
              >
                <div className={`w-12 h-12 ${s.bg} rounded-xl flex items-center justify-center mb-5`}>
                  <s.icon className={`w-6 h-6 ${s.color}`} />
                </div>
                <h3 className="text-lg font-bold text-gray-900 mb-2">{s.title}</h3>
                <p className="text-gray-500 text-sm leading-relaxed">{s.description}</p>
                <div className={`mt-5 flex items-center gap-1.5 text-sm font-medium ${s.color} opacity-0 group-hover:opacity-100 transition-opacity`}>
                  Saber mas <ArrowRight className="w-4 h-4" />
                </div>
              </div>
            ))}
          </div>
        </div>
      </section>

      {/* QUICK & AFFORDABLE SERVICES */}
      <section id="servicios-rapidos" className="py-24 bg-white">
        <div className="max-w-7xl mx-auto px-6">
          <div
            ref={quickServicesSection.ref}
            className={`text-center mb-16 transition-all duration-700 ${
              quickServicesSection.visible ? 'opacity-100 translate-y-0' : 'opacity-0 translate-y-6'
            }`}
          >
            <span className="inline-block text-sky-600 font-semibold text-sm uppercase tracking-widest mb-3">
              Servicios Rapidos y Accesibles
            </span>
            <h2 className="text-4xl md:text-5xl font-extrabold text-gray-900 mb-4 leading-tight">
              Soluciones rapidas,
              <br />
              <span className="text-sky-500">resultados inmediatos</span>
            </h2>
            <p className="text-lg text-gray-500 max-w-2xl mx-auto">
              No necesitas un plan completo para empezar. Estos servicios individuales te dan
              impacto rapido a precios accesibles.
            </p>
          </div>

          <div className="grid sm:grid-cols-2 lg:grid-cols-3 gap-6">
            {quickServices.map((s, i) => {
              const Icon = s.icon;
              return (
                <div
                  key={i}
                  style={{ transitionDelay: `${i * 80}ms` }}
                  className={`group relative bg-white rounded-2xl border ${s.border} p-7 hover:shadow-xl hover:-translate-y-1 transition-all duration-300 overflow-hidden ${
                    quickServicesSection.visible ? 'opacity-100 translate-y-0' : 'opacity-0 translate-y-8'
                  }`}
                >
                  {/* Gradient accent top bar */}
                  <div className={`absolute top-0 left-0 right-0 h-1 bg-gradient-to-r ${s.color}`} />

                  <div className={`w-12 h-12 ${s.bg} rounded-xl flex items-center justify-center mb-5`}>
                    <Icon className="w-6 h-6" />
                  </div>
                  <h3 className="text-lg font-bold text-gray-900 mb-2">{s.name}</h3>
                  <p className="text-gray-500 text-sm leading-relaxed mb-5">{s.desc}</p>
                  <div className="flex items-end justify-between">
                    <div className="flex items-end gap-1">
                      <span className="text-3xl font-extrabold text-gray-900">{s.price}</span>
                      <span className="text-gray-400 text-sm mb-1">CLP</span>
                    </div>
                    <a
                      href={`${WHATSAPP_LINK}?text=${encodeURIComponent(`Hola, me interesa el servicio de ${s.name} ($${s.price} CLP). Quiero mas informacion.`)}`}
                      target="_blank"
                      rel="noopener noreferrer"
                      className="inline-flex items-center gap-1.5 bg-emerald-500 hover:bg-emerald-600 text-white text-sm font-semibold px-4 py-2 rounded-xl transition-all hover:shadow-md active:scale-95"
                    >
                      <MessageCircle className="w-4 h-4" />
                      Lo quiero
                    </a>
                  </div>
                </div>
              );
            })}
          </div>
        </div>
      </section>

      {/* BENEFITS BANNER */}
      <section
        ref={benefitsSection.ref}
        className={`py-20 bg-gradient-to-r from-sky-600 to-cyan-500 transition-all duration-700 ${
          benefitsSection.visible ? 'opacity-100' : 'opacity-0'
        }`}
      >
        <div className="max-w-7xl mx-auto px-6">
          <div className="grid md:grid-cols-3 gap-8 items-center">
            <div className="md:col-span-1">
              <h2 className="text-3xl font-extrabold text-white mb-3 leading-tight">
                Resultados reales,
                <br />
                no promesas.
              </h2>
              <p className="text-sky-100 text-sm leading-relaxed">
                Trabajamos con PyMEs de todos los sectores. Estos son resultados promedio de
                nuestros clientes tras los primeros 90 dias.
              </p>
            </div>
            <div className="md:col-span-2 grid grid-cols-2 gap-5">
              {[
                { icon: TrendingUp, value: '3x', label: 'Productividad del equipo' },
                { icon: Zap, value: '-40%', label: 'Costos operativos' },
                { icon: Users, value: '+65%', label: 'Satisfaccion de clientes' },
                { icon: BarChart3, value: '2x', label: 'Velocidad de cierre de ventas' },
              ].map((item, i) => (
                <div key={i} className="bg-white/15 backdrop-blur-sm border border-white/20 rounded-2xl p-5">
                  <item.icon className="w-6 h-6 text-white mb-3 opacity-80" />
                  <div className="text-3xl font-extrabold text-white mb-1">{item.value}</div>
                  <div className="text-sky-100 text-sm">{item.label}</div>
                </div>
              ))}
            </div>
          </div>
        </div>
      </section>

      {/* HOW IT WORKS */}
      <section id="proceso" className="py-24 bg-white">
        <div className="max-w-7xl mx-auto px-6">
          <div
            ref={stepsSection.ref}
            className={`text-center mb-16 transition-all duration-700 ${
              stepsSection.visible ? 'opacity-100 translate-y-0' : 'opacity-0 translate-y-6'
            }`}
          >
            <span className="inline-block text-sky-600 font-semibold text-sm uppercase tracking-widest mb-3">
              Como Funciona
            </span>
            <h2 className="text-4xl md:text-5xl font-extrabold text-gray-900 mb-4 leading-tight">
              De cero a resultados
              <br />
              <span className="text-sky-500">en 4 pasos simples</span>
            </h2>
            <p className="text-lg text-gray-500 max-w-xl mx-auto">
              Sin tecnicismos, sin riesgos. Te acompañamos en cada etapa del proceso.
            </p>
          </div>

          <div className="grid md:grid-cols-2 lg:grid-cols-4 gap-6">
            {steps.map((step, i) => (
              <div
                key={i}
                style={{ transitionDelay: `${i * 100}ms` }}
                className={`relative transition-all duration-700 ${
                  stepsSection.visible ? 'opacity-100 translate-y-0' : 'opacity-0 translate-y-8'
                }`}
              >
                {i < steps.length - 1 && (
                  <div className="hidden lg:block absolute top-8 left-full w-full h-px bg-gradient-to-r from-sky-200 to-transparent z-0" />
                )}
                <div className="relative z-10 bg-gray-50 rounded-2xl p-7 h-full border border-gray-100 hover:border-sky-200 hover:shadow-lg transition-all">
                  <div className="text-5xl font-extrabold text-sky-100 mb-4 leading-none">{step.num}</div>
                  <h3 className="text-base font-bold text-gray-900 mb-2">{step.title}</h3>
                  <p className="text-sm text-gray-500 leading-relaxed">{step.desc}</p>
                </div>
              </div>
            ))}
          </div>
        </div>
      </section>

      {/* TESTIMONIALS */}
      <section id="testimonios" className="py-24 bg-slate-900">
        <div className="max-w-7xl mx-auto px-6">
          <div
            ref={testimonialsSection.ref}
            className={`text-center mb-16 transition-all duration-700 ${
              testimonialsSection.visible ? 'opacity-100 translate-y-0' : 'opacity-0 translate-y-6'
            }`}
          >
            <span className="inline-block text-sky-400 font-semibold text-sm uppercase tracking-widest mb-3">
              Testimonios
            </span>
            <h2 className="text-4xl md:text-5xl font-extrabold text-white mb-4 leading-tight">
              PyMEs que ya dieron
              <br />
              <span className="text-sky-400">el salto digital</span>
            </h2>
          </div>

          <div className="grid md:grid-cols-3 gap-6">
            {testimonials.map((t, i) => (
              <div
                key={i}
                style={{ transitionDelay: `${i * 120}ms` }}
                className={`bg-slate-800 border border-slate-700 rounded-2xl p-7 hover:border-sky-500/40 hover:shadow-xl hover:shadow-sky-900/20 transition-all duration-300 ${
                  testimonialsSection.visible ? 'opacity-100 translate-y-0' : 'opacity-0 translate-y-8'
                }`}
              >
                <div className="flex gap-1 mb-5">
                  {Array.from({ length: t.stars }).map((_, j) => (
                    <Star key={j} className="w-4 h-4 fill-amber-400 text-amber-400" />
                  ))}
                </div>
                <p className="text-slate-300 text-sm leading-relaxed mb-6 italic">"{t.text}"</p>
                <div className="flex items-center gap-3">
                  <div className="w-10 h-10 rounded-full bg-gradient-to-br from-sky-500 to-cyan-400 flex items-center justify-center text-white font-bold text-sm">
                    {t.name.charAt(0)}
                  </div>
                  <div>
                    <div className="text-white font-semibold text-sm">{t.name}</div>
                    <div className="text-slate-500 text-xs">{t.role}</div>
                  </div>
                </div>
              </div>
            ))}
          </div>
        </div>
      </section>

      {/* PRICING */}
      <section id="precios" className="py-24 bg-gray-50">
        <div className="max-w-7xl mx-auto px-6">
          <div className="text-center mb-16">
            <span className="inline-block text-sky-600 font-semibold text-sm uppercase tracking-widest mb-3">
              Planes y Precios
            </span>
            <h2 className="text-4xl md:text-5xl font-extrabold text-gray-900 mb-4 leading-tight">
              Soluciones accesibles
              <br />
              <span className="text-sky-500">para cada PyME</span>
            </h2>
            <p className="text-lg text-gray-500 max-w-2xl mx-auto">
              Precios en pesos chilenos. Sin sorpresas, sin clausulas de permanencia.
              Puedes cambiar o cancelar en cualquier momento.
            </p>
          </div>

          <div className="grid md:grid-cols-3 gap-6 mb-16">
            {plans.map((plan, i) => {
              const Icon = plan.icon;
              const isPopular = plan.popular;
              return (
                <div
                  key={i}
                  className={`relative rounded-2xl transition-all duration-300 hover:-translate-y-1 hover:shadow-2xl ${
                    isPopular
                      ? 'bg-white border-2 border-sky-500 shadow-xl shadow-sky-100 scale-[1.02] md:scale-105'
                      : 'bg-white border border-gray-200 hover:border-sky-200'
                  }`}
                >
                  {isPopular && (
                    <div className="absolute -top-4 left-1/2 -translate-x-1/2 bg-sky-500 text-white text-xs font-bold px-5 py-1.5 rounded-full shadow-lg shadow-sky-200">
                      MAS ELEGIDO
                    </div>
                  )}
                  <div className="p-7">
                    <div className="flex items-center gap-3 mb-6">
                      <div
                        className={`w-10 h-10 rounded-xl flex items-center justify-center ${
                          isPopular ? 'bg-sky-500' : 'bg-sky-50'
                        }`}
                      >
                        <Icon className={`w-5 h-5 ${isPopular ? 'text-white' : 'text-sky-500'}`} />
                      </div>
                      <div>
                        <h3 className="font-bold text-gray-900">{plan.name}</h3>
                        <p className="text-xs text-gray-400">{plan.subtitle}</p>
                      </div>
                    </div>

                    <div className="mb-6">
                      <div className="flex items-end gap-1">
                        <span className="text-4xl font-extrabold text-gray-900">{plan.price}</span>
                        <span className="text-gray-400 text-sm mb-1">{plan.period}</span>
                      </div>
                      {plan.period === '/mes' && (
                        <p className="text-xs text-gray-400 mt-1">Facturacion mensual. Descuento por pago anual.</p>
                      )}
                    </div>

                    <div className="space-y-3 mb-8">
                      {plan.features.map((f, j) => (
                        <div key={j} className="flex items-start gap-2.5">
                          <CheckCircle className="w-4 h-4 text-sky-500 mt-0.5 shrink-0" />
                          <span className="text-sm text-gray-600">{f}</span>
                        </div>
                      ))}
                    </div>

                    <a
                      href={`${WHATSAPP_LINK}?text=${encodeURIComponent(`Hola, me interesa el plan ${plan.name} ($${plan.price}/mes). Quiero comenzar.`)}`}
                      target="_blank"
                      rel="noopener noreferrer"
                      className={`block w-full py-3.5 rounded-xl font-bold text-sm text-center transition-all hover:shadow-lg active:scale-95 ${
                        isPopular
                          ? 'bg-sky-500 hover:bg-sky-600 text-white shadow-md shadow-sky-200'
                          : 'bg-gray-900 hover:bg-gray-800 text-white'
                      }`}
                    >
                      Elegir {plan.name}
                    </a>
                  </div>
                </div>
              );
            })}
          </div>

          {/* Extra a la carta services */}
          <div className="max-w-5xl mx-auto">
            <h3 className="text-2xl font-extrabold text-gray-900 text-center mb-3">
              Servicios adicionales a la carta
            </h3>
            <p className="text-gray-500 text-center mb-10">
              Agrega estos servicios a cualquier plan o contratalos de forma independiente.
            </p>
            <div className="grid sm:grid-cols-2 gap-5">
              {extraServices.map((s, i) => {
                const Icon = s.icon;
                return (
                  <div
                    key={i}
                    className="bg-white rounded-2xl border border-gray-200 p-6 flex items-start gap-4 hover:border-sky-200 hover:shadow-md transition-all"
                  >
                    <div className="w-10 h-10 bg-sky-50 rounded-xl flex items-center justify-center shrink-0">
                      <Icon className="w-5 h-5 text-sky-500" />
                    </div>
                    <div className="flex-1">
                      <div className="flex items-center justify-between mb-1">
                        <h4 className="font-bold text-gray-900 text-sm">{s.name}</h4>
                        <span className="text-xs bg-sky-50 text-sky-600 font-semibold px-2 py-0.5 rounded-md">
                          {s.tag}
                        </span>
                      </div>
                      <p className="text-xs text-gray-400 mb-2">{s.desc}</p>
                      <span className="text-lg font-extrabold text-gray-900">{s.price}</span>
                    </div>
                  </div>
                );
              })}
            </div>
          </div>
        </div>
      </section>

      {/* CTA BAND */}
      <section className="py-20 bg-gradient-to-br from-sky-50 to-cyan-50 border-y border-sky-100">
        <div className="max-w-4xl mx-auto px-6 text-center">
          <div className="w-16 h-16 bg-sky-500 rounded-2xl flex items-center justify-center mx-auto mb-6 shadow-lg shadow-sky-200">
            <Rocket className="w-8 h-8 text-white" />
          </div>
          <h2 className="text-4xl font-extrabold text-gray-900 mb-4 leading-tight">
            Agenda tu diagnostico
            <span className="text-sky-500"> 100% gratis</span>
          </h2>
          <p className="text-gray-500 text-lg mb-8 max-w-xl mx-auto">
            45 minutos, sin compromisos. Descubre exactamente que soluciones de IA pueden
            transformar tu negocio este mes.
          </p>
          <div className="flex flex-col sm:flex-row gap-4 justify-center">
            <a
              href={WHATSAPP_DIAGNOSTICO}
              target="_blank"
              rel="noopener noreferrer"
              className="group inline-flex items-center justify-center gap-2 bg-sky-500 hover:bg-sky-600 text-white font-bold px-8 py-4 rounded-2xl transition-all hover:shadow-xl hover:shadow-sky-200 hover:-translate-y-0.5"
            >
              Reservar Ahora
              <ArrowRight className="w-5 h-5 group-hover:translate-x-1 transition-transform" />
            </a>
            <a
              href={WHATSAPP_LINK}
              target="_blank"
              rel="noopener noreferrer"
              className="inline-flex items-center justify-center gap-2 bg-emerald-500 hover:bg-emerald-600 text-white font-bold px-8 py-4 rounded-2xl transition-all hover:shadow-xl hover:shadow-emerald-200 hover:-translate-y-0.5"
            >
              <MessageCircle className="w-5 h-5" />
              WhatsApp Directo
            </a>
          </div>
        </div>
      </section>

      {/* CONTACT */}
      <section id="contacto" className="py-24 bg-white">
        <div className="max-w-7xl mx-auto px-6">
          <div className="grid lg:grid-cols-2 gap-16 items-start">
            {/* Left */}
            <div
              ref={contactSection.ref}
              className={`transition-all duration-700 ${
                contactSection.visible ? 'opacity-100 translate-x-0' : 'opacity-0 -translate-x-8'
              }`}
            >
              <span className="inline-block text-sky-600 font-semibold text-sm uppercase tracking-widest mb-3">
                Contacto
              </span>
              <h2 className="text-4xl font-extrabold text-gray-900 mb-5 leading-tight">
                Hablemos sobre
                <br />
                <span className="text-sky-500">tu negocio</span>
              </h2>
              <p className="text-gray-500 leading-relaxed mb-10">
                Cuéntanos tus desafios y objetivos. Nuestro equipo te respondera en menos de 24
                horas con una propuesta concreta.
              </p>

              <div className="space-y-5">
                <a
                  href="mailto:impulsamostupymeia@gmail.com"
                  className="flex items-center gap-4 hover:bg-sky-50 -mx-1 px-1 py-1 rounded-xl transition-colors"
                >
                  <div className="w-11 h-11 bg-sky-50 rounded-xl flex items-center justify-center">
                    <Mail className="w-5 h-5 text-sky-600" />
                  </div>
                  <div>
                    <div className="text-xs text-gray-400 font-medium">Email</div>
                    <div className="text-gray-800 font-semibold text-sm">impulsamostupymeia@gmail.com</div>
                  </div>
                </a>
                <a
                  href={`tel:+${WHATSAPP_NUMBER}`}
                  className="flex items-center gap-4 hover:bg-sky-50 -mx-1 px-1 py-1 rounded-xl transition-colors"
                >
                  <div className="w-11 h-11 bg-sky-50 rounded-xl flex items-center justify-center">
                    <Phone className="w-5 h-5 text-sky-600" />
                  </div>
                  <div>
                    <div className="text-xs text-gray-400 font-medium">Telefono</div>
                    <div className="text-gray-800 font-semibold text-sm">+56921733645</div>
                  </div>
                </a>
                <a
                  href={WHATSAPP_LINK}
                  target="_blank"
                  rel="noopener noreferrer"
                  className="flex items-center gap-4 hover:bg-emerald-50 -mx-1 px-1 py-1 rounded-xl transition-colors"
                >
                  <div className="w-11 h-11 bg-emerald-50 rounded-xl flex items-center justify-center">
                    <MessageCircle className="w-5 h-5 text-emerald-600" />
                  </div>
                  <div>
                    <div className="text-xs text-gray-400 font-medium">WhatsApp</div>
                    <div className="text-emerald-700 font-semibold text-sm">+56921733645 — Chat directo</div>
                  </div>
                </a>
              </div>

              <div className="mt-10 bg-sky-50 rounded-2xl p-6 border border-sky-100">
                <div className="flex items-center gap-3 mb-3">
                  <CheckCircle className="w-5 h-5 text-sky-500" />
                  <span className="font-semibold text-gray-800">Diagnostico sin costo</span>
                </div>
                <div className="flex items-center gap-3 mb-3">
                  <CheckCircle className="w-5 h-5 text-sky-500" />
                  <span className="font-semibold text-gray-800">Sin contratos de largo plazo</span>
                </div>
                <div className="flex items-center gap-3">
                  <CheckCircle className="w-5 h-5 text-sky-500" />
                  <span className="font-semibold text-gray-800">Resultados medibles en 30 dias</span>
                </div>
              </div>
            </div>

            {/* Form */}
            <div
              className={`transition-all duration-700 delay-200 ${
                contactSection.visible ? 'opacity-100 translate-x-0' : 'opacity-0 translate-x-8'
              }`}
            >
              {submitted ? (
                <div className="bg-emerald-50 border border-emerald-200 rounded-2xl p-10 text-center">
                  <CheckCircle className="w-16 h-16 text-emerald-500 mx-auto mb-4" />
                  <h3 className="text-2xl font-extrabold text-gray-900 mb-2">Mensaje Enviado!</h3>
                  <p className="text-gray-500">
                    Te contactaremos en menos de 24 horas con tu diagnostico personalizado.
                  </p>
                </div>
              ) : (
                <form
                  onSubmit={handleSubmit}
                  className="bg-gray-50 rounded-2xl border border-gray-200 p-8 space-y-5"
                >
                  <div className="grid sm:grid-cols-2 gap-5">
                    <div>
                      <label className="block text-sm font-semibold text-gray-700 mb-1.5">
                        Nombre *
                      </label>
                      <input
                        required
                        value={formData.name}
                        onChange={(e) => setFormData({ ...formData, name: e.target.value })}
                        placeholder="Tu nombre"
                        className="w-full border border-gray-200 rounded-xl px-4 py-3 text-sm focus:outline-none focus:ring-2 focus:ring-sky-400 focus:border-transparent bg-white transition"
                      />
                    </div>
                    <div>
                      <label className="block text-sm font-semibold text-gray-700 mb-1.5">
                        Email *
                      </label>
                      <input
                        required
                        type="email"
                        value={formData.email}
                        onChange={(e) => setFormData({ ...formData, email: e.target.value })}
                        placeholder="tu@empresa.com"
                        className="w-full border border-gray-200 rounded-xl px-4 py-3 text-sm focus:outline-none focus:ring-2 focus:ring-sky-400 focus:border-transparent bg-white transition"
                      />
                    </div>
                  </div>
                  <div>
                    <label className="block text-sm font-semibold text-gray-700 mb-1.5">
                      Telefono / WhatsApp
                    </label>
                    <input
                      value={formData.phone}
                      onChange={(e) => setFormData({ ...formData, phone: e.target.value })}
                      placeholder="+56 9 ..."
                      className="w-full border border-gray-200 rounded-xl px-4 py-3 text-sm focus:outline-none focus:ring-2 focus:ring-sky-400 focus:border-transparent bg-white transition"
                    />
                  </div>
                  <div>
                    <label className="block text-sm font-semibold text-gray-700 mb-1.5">
                      Cuentanos sobre tu negocio *
                    </label>
                    <textarea
                      required
                      rows={4}
                      value={formData.message}
                      onChange={(e) => setFormData({ ...formData, message: e.target.value })}
                      placeholder="Que tipo de negocio tienes? Cuales son tus principales desafios?"
                      className="w-full border border-gray-200 rounded-xl px-4 py-3 text-sm focus:outline-none focus:ring-2 focus:ring-sky-400 focus:border-transparent bg-white transition resize-none"
                    />
                  </div>
                  <button
                    type="submit"
                    className="w-full bg-sky-500 hover:bg-sky-600 text-white font-bold py-4 rounded-xl transition-all hover:shadow-lg hover:shadow-sky-200 active:scale-98 flex items-center justify-center gap-2 group"
                  >
                    Solicitar Diagnostico Gratis
                    <ArrowRight className="w-5 h-5 group-hover:translate-x-1 transition-transform" />
                  </button>
                  <p className="text-center text-xs text-gray-400">
                    Al enviar aceptas nuestra politica de privacidad. Sin spam, prometido.
                  </p>
                </form>
              )}
            </div>
          </div>
        </div>
      </section>

      {/* FOOTER */}
      <footer className="bg-slate-900 text-slate-400 py-12">
        <div className="max-w-7xl mx-auto px-6">
          <div className="grid md:grid-cols-4 gap-8 mb-10">
            <div className="md:col-span-2">
              <div className="flex items-center gap-2.5 mb-4">
                <div className="w-8 h-8 rounded-lg bg-gradient-to-br from-sky-500 to-cyan-400 flex items-center justify-center">
                  <Rocket className="w-4 h-4 text-white" />
                </div>
                <span className="text-white font-bold">
                  ImpulsamosTuPyme<span className="text-cyan-400">IA</span>
                </span>
              </div>
              <p className="text-sm leading-relaxed max-w-xs">
                Transformamos PyMEs con inteligencia artificial. Mas productividad, menos costos,
                mas crecimiento.
              </p>
            </div>
            <div>
              <h4 className="text-white font-semibold text-sm mb-4">Servicios</h4>
              <ul className="space-y-2 text-sm">
                {['Automatizacion con IA', 'Analisis de Datos', 'Chatbots', 'Presencia Digital', 'Logo y Video', 'Flyer y Tarjetas'].map(
                  (s) => (
                    <li key={s}>
                      <button onClick={() => scrollTo('servicios')} className="hover:text-sky-400 transition-colors">{s}</button>
                    </li>
                  )
                )}
              </ul>
            </div>
            <div>
              <h4 className="text-white font-semibold text-sm mb-4">Empresa</h4>
              <ul className="space-y-2 text-sm">
                {[
                  { label: 'Nosotros', id: 'hero' },
                  { label: 'Precios', id: 'precios' },
                  { label: 'Contacto', id: 'contacto' },
                ].map((s) => (
                  <li key={s.label}>
                    <button onClick={() => scrollTo(s.id)} className="hover:text-sky-400 transition-colors">{s.label}</button>
                  </li>
                ))}
                <li>
                  <a href={WHATSAPP_LINK} target="_blank" rel="noopener noreferrer" className="text-emerald-400 hover:text-emerald-300 transition-colors">
                    WhatsApp
                  </a>
                </li>
                <li>
                  <a href="mailto:impulsamostupymeia@gmail.com" className="hover:text-sky-400 transition-colors">
                    impulsamostupymeia@gmail.com
                  </a>
                </li>
              </ul>
            </div>
          </div>
          <div className="border-t border-slate-800 pt-6 flex flex-col sm:flex-row items-center justify-between gap-4">
            <p className="text-xs">
              &copy; {new Date().getFullYear()} ImpulsamosTuPymeIA. Todos los derechos reservados.
            </p>
            <div className="flex gap-4 text-xs">
              <button className="hover:text-sky-400 transition-colors">Privacidad</button>
              <button className="hover:text-sky-400 transition-colors">Terminos</button>
            </div>
          </div>
        </div>
      </footer>

      {/* WhatsApp float button */}
      <a
        href={WHATSAPP_LINK}
        target="_blank"
        rel="noopener noreferrer"
        className="fixed bottom-6 right-6 z-50 w-14 h-14 bg-emerald-500 hover:bg-emerald-400 rounded-full flex items-center justify-center shadow-2xl shadow-emerald-500/40 transition-all hover:scale-110 active:scale-95"
        title="Hablar por WhatsApp +56921733645"
      >
        <MessageCircle className="w-6 h-6 text-white fill-white" />
      </a>
    </div>
  );
}
